#!/bin/bash

if [ -e ~/.oh-my-zsh ]; then
    echo "Deno was already installed, so the installation was skipped"
else

    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

fi
